#!/bin/sh

echo "Running pre-push UI tests..."
pnpm run test:ui
TEST_EXIT_CODE=$?

if [ $TEST_EXIT_CODE -ne 0 ]; then
  echo "UI Tests failed with code $TEST_EXIT_CODE"
  exit $TEST_EXIT_CODE
fi

echo "Pre-push UI tests passed!"
exit 0
