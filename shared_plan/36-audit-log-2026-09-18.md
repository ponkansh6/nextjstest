# Plan36 監査ログ

## 日付

2026-09-18（Asia/Tokyo）

## 対象

`data/source/official-cti-2025-long-term/` と、これを記録するplan36仕様同期。対象はe-Stat current 2025年基準 CTIミクロ基本系列・二人以上世帯の原数値（`statInfId=000040499070`）および季節調整値（`statInfId=000040499082`）。公式 `fileKind=0` のXLSXを入力とした。認証情報、appIdの値、appId環境変数名はこのログに記録しない。

## 選定根拠

公式メタデータと原表の表題、基準年、対象世帯、CTIミクロ基本系列、原数値/季節調整値、提供形式を照合した。旧IDまたは誤ったfileKindの候補は採用せず、現行2 IDを採用した。季節調整表は公式コード行を持たないため、`official_series_code=null`を維持した。

## 検証結果

- raw公式XLSX: 2 variant、source range 2002-01〜2026-07
- normalized adopted range: 2005-01〜2026-07
- 各variant: 22系列×259月=5698 rows
- normalized schema: `variant,series_index,official_series_code,series_name,month,raw_value,is_missing`
- 成果物: raw XLSX 2、normalized CSV 2、metadata 2、manifest、series-map 44 rows、representative-snapshot 132 rows
- SHA-256: 全成果物で検証済み。manifest相互参照も検証済み
- parser/target sheet/title/header/order/identity/period/duplicate/continuity/schema検証: PASS
- HTML/404/empty/unknown reject: PASS
- `-`欠損とnumeric 0の区別、補間/丸め/異基準接続なし: PASS
- retry/timeout、appId非露出、同一filesystem staging、atomic publishと失敗時の既存成果物保持: PASS
- plan36専用テスト: 17/17 PASS
- type-check: PASS
- lint: PASS（既存5 warningsのみ、errorsなし）
- build: PASS
- CTI regression: PASS

## 実行コマンド（秘密なし）

実行したコマンドは次のとおり（秘密情報なし）。

- `node scripts/fetch-cti-basic-series-2025.mjs`
- `pnpm exec vitest run tests/data-quality/cti-basic-series-2025.test.ts`
- `pnpm type-check`
- `pnpm lint`
- `pnpm build`
- CTI regression runner（既存のCTI回帰検証コマンド）

取得runnerには公式XLSX取得、XLSX検証、正規化、manifest/SHA-256検証、atomic publishを含む。ネットワーク取得には公式 `fileKind=0` URLを使用した。appId値や認証情報を含むコマンド・出力は記録していない。

## 失敗 → 修正 → 再実行

1. 初回の実ネットワーク取得はparser、endpoint、EXDEVで失敗した。
2. 公式endpoint/`fileKind=0` URLを確定し、対象sheet/title/header判定とXLSX parserを修正した。同一filesystem stagingへ変更し、atomic publishの失敗保持を確保した。
3. 旧ID/fileKind候補を除外して現行2 IDを再取得・再検証した。
4. 最終実行は成功し、期間、件数、系列数、schema、SHA-256、manifest相互参照、専用テスト17/17を確認した。

## 最終判定

PASS。plan36の取得・監査・仕様同期は完了。残るのは既存lintの5 warningsのみで、新たなerrorはない。成果物は既存loader/UIへ未接続であり、既存2025候補および2020 rollbackの採用経路と分離されている。
